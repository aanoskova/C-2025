//Дан текст. Написать программу,печатающую только нечетные строки введенного текста. 

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main()
{
    FILE* aa;
    FILE* bb;
    fopen_s(&aa, "text.txt", "r");
    fopen_s(&bb, "read.txt", "w");
    char c;
    int n = 1;
    while ((c = fgetc(aa)) != EOF) {
        if (c == '\n') {
            n = n + 1;
        }
        if (n % 2 == 1) {
            fputc(c, bb);
        }
    }
    fclose(aa);
    fclose(bb);
    return 0;
}


//С проверкой
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
    FILE* aa = NULL;   
    FILE* bb = NULL;   
    char c;
    int n = 1;       

    if (fopen_s(&aa, "text.txt", "r") != 0) {
        printf("file is not open 'text.txt' for read.\n");
        return 1; 
    }

    if (fopen_s(&bb, "read.txt", "w") != 0) {
        printf("file is not open 'read.txt' for write.\n");
        fclose(aa);  
        return 1;  
    }

    while ((c = fgetc(aa)) != EOF) {
       
        if (c == '\n') {
            n++;
        }

        if (n % 2 == 1) {
            fputc(c, bb);
        }
    }

    
    if (aa != NULL) {
        fclose(aa);
    }
    if (bb != NULL) {
        fclose(bb);
    }

    printf("Obrabotka zavershena. Nechetnye stroki zapisany v 'read.txt'.\n");
    return 0;
}

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
    FILE* aa = NULL;   // Входной файл
    FILE* bb = NULL;   // Выходной файл
    char c;
    int n = 1;       // Счётчик строк (начинаем с 1 — нечётная)


    // 1. Открываем входной файл для чтения
    if (fopen_s(&aa, "text.txt", "r") != 0) {
        printf("Ошибка: не удалось открыть файл 'text.txt' для чтения.\n");
        return 1;  // Код ошибки
    }

    // 2. Открываем выходной файл для записи
    if (fopen_s(&bb, "read.txt", "w") != 0) {
        printf("Ошибка: не удалось создать/открыть файл 'read.txt' для записи.\n");
        fclose(aa);  // Закрываем уже открытый входной файл
        return 1;  // Код ошибки
    }

    // 3. Читаем файл посимвольно до конца (EOF)
    while ((c = fgetc(aa)) != EOF) {
        // Если встретили символ перевода строки — увеличиваем номер текущей строки
        if (c == '\n') {
            n++;
        }

        // Если текущая строка нечётная — записываем символ в выходной файл
        if (n % 2 == 1) {
            fputc(c, bb);
        }
    }

    // 4. Закрываем файлы
    if (aa != NULL) {
        fclose(aa);
    }
    if (bb != NULL) {
        fclose(bb);
    }

    printf("Обработка завершена. Нечётные строки записаны в 'read.txt'.\n");
    return 0;  // Успешное завершение
}


#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main()
{
    FILE *bb, *aa;
    char c;
    int n = 1;

    fopen_s(&bb, "text.txt", "w");
    printf("vvedite text: \n");

    while (scanf_s("%c", &c, 1) != EOF) {
        fputc(c, bb);
    }
    fclose(bb);

    fgetc(aa);




    fopen_s(&aa, "text.txt", "r");
    fopen_s(&bb, "read.txt", "w");

//нечетные строки введенного текста
    while (fscanf_s(aa, "%c", &c, 1) != EOF) {
        if (n % 2 == 1) {
            printf("%c", c);
            fprintf(bb, "%c", c);
        }

        if (c == '\n') {
            n = n + 1;
        }
    }
    fprintf(bb, "\n Kolichestvo strok: %d", n);

    fclose(aa);
    fclose(bb);

    return 0;
}
